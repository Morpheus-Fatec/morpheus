package fatec.morpheus.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import fatec.morpheus.entity.MapSourceApi;

public interface MapSourceApiRepository extends JpaRepository<MapSourceApi, Integer> {
    
}
